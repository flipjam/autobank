---
id: TASK_0002
state: completed
phase: done
created_at: 2026-07-28T00:01:26.394Z
updated_at: 2026-07-28T00:29:08.648Z
title: Use the authorized faster subscription-backed model for this bounded setup/testing task only, in `C:\Projects\autobank` (`flipjam/autobank`, expected branch `main`), so every model interaction for the current test uses that approved subscription model while treating `REQ-20260727-002` as non-blocking for this test run.
label: Use approved subscription model for autobank test (REQ-20260727-002)
---

## raw prompt

Use the authorized faster subscription-backed model for this bounded test.

## refined prompt

GOAL
Use the authorized faster subscription-backed model for this bounded setup/testing task only, in `C:\Projects\autobank` (`flipjam/autobank`, expected branch `main`), so every model interaction for the current test uses that approved subscription model while treating `REQ-20260727-002` as non-blocking for this test run.

CONSTRAINTS
- This step is limited to the bounded setup/testing slice and must not perform campaign/revenue work or any external side effects beyond repo-local test actions.
- Use the faster subscription-backed model authorized under Rule 7; do not use any other model for this step.
- Use one active model request at a time.
- `REQ-20260727-002` remains a production-readiness gate: it may block production activation but does not block this setup/testing task.
- Do not alter repository governance files for this step unless required by the immediate task; preserve existing state semantics and avoid unrelated edits.

KNOWN-UNKNOWNS
- What exact model identifier (provider/model name or API target) is the authorized “faster subscription-backed model” for this session?
- Where is the authorization source configured (env var, tool config, or prompt context) so the agent can explicitly confirm the selected model?
- If the authorized faster model is unavailable during execution, should the step fail immediately or request re-authorization before retrying?
- Is there a required log format or evidence artifact in which model selection must be recorded for this step?

EXTERNAL-DEPENDENCIES
- Subscription LLM Provider  authorized high-throughput subscription-backed model endpoint used for bounded test model requests

## verified tooling

(none verified)

## research

FILES
.pi-tasks/TASK_0002.md  Task contract for this exact model-selection step, including constraints, non-blocking REQ handling, and scope.
.pi-tasks/TASK_AUTO_0001.md  Parent task context/checklist for the restart-and-resume run that includes this subtask.
.pi-tasks/contracts.md  Authoritative required procedure and pass criteria tied to this task family.
.pi-tasks/requirements.md  High-level objective for the wider resume/recovery task this step belongs to.
.pi-tasks/requirements-owned.md  Owned-file contract and required read list for state/consistency documents.
.pi-tasks/env-notes.md  Logged environment/task context notes used for continuity checks.
AGENTS.md  Startup read-order and execution rules, including one active model request policy.
AUTOBANK_POLICY.md  Rule 7 and other safety/governance constraints for model usage.
CONTROL.json  Governing control state and external-action gate required for task safety checks.
MISSION.md  Current phase/objective context for determining allowed work.
AUTHORIZED_RESOURCES.md  Approved resources and outside-model allowances for bounded setup.
OWNER_REQUESTS.md  Current open request REQ-20260727-002 and non-blocking status for setup/testing.
STATE.json  Current task/request state and active control/tracking snapshot.
RUNBOOK.md  Startup/recovery/checkpoint rules and side-effect limits.
SETUP_AUDIT_REPORT.md  Existing audit-state snapshot including provider/model and authorization context.
RESUME_TEST_PLAN.md  Current resume/test plan state used for this bounded slice.
RESUME_TEST_REPORT.md  Current bounded test report scaffold/result file to reflect this pass.
DECISION_LOG.md  Recent decision DEC-20260727-004 confirming setup/testing subscription-model allowance.
EVIDENCE_INDEX.md  Evidence registry for any model-selection or model-use logging artifacts.
WEB_ACCESS_CALIBRATION_LOG.md  Reference for temporary subscription-backed access constraints and prior calibration limits.
C:/Users/Administrator/.pi/agent/settings.json  Current default Pi provider/model configuration (openai-codex, gpt-5.3-codex-spark).
C:/Users/Administrator/.pi/agent/models-store.json  Subscription provider model catalog used to confirm authorized faster model target.
C:/Users/Administrator/.pi/agent/models.json  Local provider model registry (including minipc) for fallback/lock context.
C:/Users/Administrator/.pi/agent/auth.json  Subscription auth data source for model availability.
C:/Users/Administrator/.pi/web-search.json  Current web-search runtime config used to confirm no extra API-key-based model routing side effects.

APIS
pi --list-models list reachable providers and model IDs available to the current Pi runtime
pi --provider openai-codex --model gpt-5.3-codex-spark --print "<prompt>" run one explicit subscription-backed model call; use for all bounded task interactions
PI_PROVIDER check environment default provider before each call or to confirm implicit routing
PI_MODEL check environment default model before each call or to confirm implicit routing
defaultProvider read from Pi agent settings to resolve the configured default provider
defaultModel read from Pi agent settings to resolve the configured default model
openai-codex provider id authorize subscription-backed provider for this setup/testing task
gpt-5.3-codex-spark model id designate the authorized faster model target for this run
modelsStore.openai-codex.models[] enumerate subscription provider catalog and verify target model availability
npm test run standard validator smoke/self-test suite for the setup/testing slice
npm run validate:strict run deterministic strict validation pass after task operations
model-unavailability policy fail hard and request owner re-authorization if chosen model/provider is unavailable; never silent fallback or auto-switch
EVIDENCE record (EVD-...) append a model-selection/interactions log entry with timestamp, provider/model source, command/proof, and result in the evidence index

CONTEXT
Yes — the repo currently supports this explicitly, and this is the **correct pattern**:

### Use subscription-backed model for this setup run
Use the faster model via explicit override (do **not** change policy files):

- Set at call time:  
  `PI_PROVIDER=openai-codex` and `PI_MODEL=gpt-5.3-codex-spark`  
  (or pass directly: `pi --provider openai-codex --model gpt-5.3-codex-spark ...`).
- This is the owner-authorized exception for bounded setup/testing.
- Keep to **one active model request at a time**.

### Keep `REQ-20260727-002` non-blocking for this run
- It stays **open** in `OWNER_REQUESTS.md` and `STATE.json.open_owner_request_ids`.
- Owner has classified it as **production-readiness only** (local minipc default + no silent metered fallback), not a setup stopper.
- `CONTROL.json` remains `setup` with `external_actions_allowed: false`.

### Architectural facts that matter
1. Runtime model sources:
   - Default configured model/provider (`settings.json`): `openai-codex` / `gpt-5.3-codex-spark`.
   - Local fallback endpoint exists (`minipc`, model `qwen3.6-35b-a3b-xl`, zero-cost).
2. `STATE`/`RUNBOOK`/`SETUP_AUDIT_REPORT`/`RESUME_TEST_PLAN` consistently treat this request as deferred gate for campaign-bound production, not setup.
3. Completion is still blocked by git sync state until `HEAD...origin/main` is `0 0` (currently ahead of remote).
4. Any additional subscription-backed calls should be logged via `EVIDENCE_INDEX.md` (and reflected in audit/report artifacts per the task rules).

VERIFIED-TOOLING
  (none verified)

## grill Q&A

(no questions produced)

## spec

GOAL
Complete the bounded setup/testing slice in `C:\Projects\autobank` (GitHub repository `flipjam/autobank`, expected branch `main`) using the authorized faster subscription-backed model (`openai-codex` + `gpt-5.3-codex-spark`) for this slice’s model interactions, while treating `REQ-20260727-002` as setup/testing non-blocking and preserving setup controls, then record a truthful checkpoint in `RESUME_TEST_REPORT.md` and evidence in `EVIDENCE_INDEX.md`.

CONSTRAINTS
  - "Faster subscription-backed models are authorized for this bounded setup/testing task under Rule 7." [opening] — owned requirement from the source design (AUTHORITATIVE; satisfy it in this task, do not narrow it)
- Stay strictly within bounded setup/testing: no campaign/revenue activity, no external system mutations outside repo-local work and Git actions on `flipjam/autobank`.
- Use only the authorized subscription-backed model path for this slice; no fallback provider/model switches and no parallel model calls.
- Use one active model request at a time.
- `REQ-20260727-002` is a production-readiness gate only; it must not block this setup/testing run.
- Keep `CONTROL.json` in setup mode (`run_state: "setup"` and `external_actions_allowed: false`).
- Do not substantively modify `AUTOBANK_POLICY.md`, `CONTROL.json`, `MISSION.md`, or `AUTHORIZED_RESOURCES.md` unless directly required by this slice.
- Any owner-controlled request text must remain in `OWNER_REQUESTS.md` only.
- Inspect the required repository state first (`AUTOBANK_POLICY.md`, `CONTROL.json`, `MISSION.md`, `AUTHORIZED_RESOURCES.md`, `AGENTS.md`, `STATE.json`, `OWNER_REQUESTS.md`, `RUNBOOK.md`, `SETUP_AUDIT_REPORT.md`, `RESUME_TEST_PLAN.md`, `DECISION_LOG.md`, `EVIDENCE_INDEX.md`, `WEB_ACCESS_CALIBRATION_LOG.md`) before changes.
- All required validation and verification is performed by the agent itself; no deferral of routine commands to the owner.

ACCEPTANCE
- Verification confirms repository context is exactly `C:\Projects/autobank` and remote identity is `flipjam/autobank`, with branch `main` and upstream `origin/main`.
- Pre-change and post-change checks are executed for: current branch/upstream, remotes, local `HEAD`, `origin/main` `HEAD`, working-tree status, and ahead/behind counts.
- Synchronization to upstream is done with fast-forward-only behavior (`--ff-only`), with no history rewrite.
- `CONTROL.json` remains `run_state: setup` and `external_actions_allowed: false` at completion.
- `REQ-20260727-002` remains open/non-blocking for setup/testing and continues as production-readiness constrained in owner-request/state artifacts (`OWNER_REQUESTS.md`, `STATE.json`).
- Model interactions for this slice explicitly use `PI_PROVIDER=openai-codex` and `PI_MODEL=gpt-5.3-codex-spark`; if unavailable, the run fails immediately and requests re-authorization.
- At least one successful `pi --list-models` and one successful model interaction are both logged as a new `EVD-...` entry in `EVIDENCE_INDEX.md` containing source/account, command, and result fields.
- `RESUME_TEST_REPORT.md` exists and records this run’s model usage context, `REQ-20260727-002` treatment, and checkpoint status.
- Required docs are read and reflected, with no prohibited substantive edits to `AUTOBANK_POLICY.md`, `CONTROL.json`, `MISSION.md`, or `AUTHORIZED_RESOURCES.md`.
- Source validation for this repo is executed: `npm test`, `npm run validate:strict`, and `npm run lint --if-present` / `npm run typecheck --if-present` (executed when available).
- Intended public-safe updates are committed and pushed to `main`, and `origin/main` is advanced to the local result commit.

VERIFY:
```sh
set -euo pipefail

cd "C:/Projects/autobank" || exit 1

fail() {
  echo "VERIFY FAIL: $*" >&2
  exit 1
}

repo_root="$(git rev-parse --show-toplevel)"
case "$repo_root" in
  *"/Projects/autobank"|"/c/Projects/autobank"|"/C/Projects/autobank"|"C:/Projects/autobank") ;;
  *) fail "Unexpected repository path: $repo_root";;
esac

git remote -v
remote_url="$(git remote get-url origin)"
case "$remote_url" in
  "https://github.com/flipjam/autobank.git"|"git@github.com:flipjam/autobank.git") ;;
  *) fail "Remote is not flipjam/autobank: $remote_url";;
esac

branch="$(git branch --show-current)"
[ "$branch" = "main" ] || fail "Expected branch main, got: $branch"

upstream="$(git rev-parse --abbrev-ref --symbolic-full-name "@{u}" 2>/dev/null || true)"
[ -n "$upstream" ] || fail "No upstream configured for branch $branch"
[ "$upstream" = "origin/main" ] || fail "Expected upstream origin/main, got: $upstream"

git rev-parse --show-toplevel
git remote -v
git branch --show-current
git rev-parse --abbrev-ref --symbolic-full-name "@{u}"
git rev-parse HEAD
git rev-parse "$upstream"
git status --short

ahead_before="$(git rev-list --count "$upstream..HEAD")"
behind_before="$(git rev-list --count "HEAD..$upstream")"
echo "Ahead/behind before sync: ${ahead_before}/${behind_before}"

for f in AUTOBANK_POLICY.md CONTROL.json MISSION.md AUTHORIZED_RESOURCES.md AGENTS.md STATE.json OWNER_REQUESTS.md RUNBOOK.md SETUP_AUDIT_REPORT.md RESUME_TEST_PLAN.md DECISION_LOG.md EVIDENCE_INDEX.md WEB_ACCESS_CALIBRATION_LOG.md; do
  [ -f "$f" ] || fail "Missing required artifact: $f"
  sed -n '1,3p' "$f" >/dev/null
done

git fetch --ff-only
git merge --ff-only "$upstream"

ahead_after="$(git rev-list --count "$upstream..HEAD")"
behind_after="$(git rev-list --count "HEAD..$upstream")"
echo "Ahead/behind after sync: ${ahead_after}/${behind_after}"
[ "$behind_after" -eq 0 ] || fail "Fast-forward sync incomplete; upstream is ahead by $behind_after"

if [ -n "$(git diff --name-only HEAD -- AUTOBANK_POLICY.md CONTROL.json MISSION.md AUTHORIZED_RESOURCES.md)" ]; then
  fail "Prohibited governance edits detected"
fi

node -e '
  const fs = require("fs");
  const control = JSON.parse(fs.readFileSync("CONTROL.json", "utf8"));
  if (control.run_state !== "setup" || control.external_actions_allowed !== false) {
    console.error("CONTROL.json does not preserve setup mode / external_actions_allowed=false");
    process.exit(1);
  }
  const state = JSON.parse(fs.readFileSync("STATE.json", "utf8"));
  if (!Array.isArray(state.open_owner_request_ids) || !state.open_owner_request_ids.includes("REQ-20260727-002")) {
    console.error("REQ-20260727-002 missing from STATE.json");
    process.exit(1);
  }
' || fail "Governance/state checks failed"

grep -n "REQ-20260727-002" OWNER_REQUESTS.md
grep -qi "production-readiness gate" OWNER_REQUESTS.md
grep -qi "setup/testing" OWNER_REQUESTS.md

if ! PI_PROVIDER=openai-codex PI_MODEL=gpt-5.3-codex-spark pi --list-models > /tmp/pi-models.txt 2>&1; then
  fail "Authorized model backend unavailable; request re-authorization and retry."
fi
grep -qi "gpt-5.3-codex-spark" /tmp/pi-models.txt || fail "Provider/model not present in model list output"

model_resp="$(PI_PROVIDER=openai-codex PI_MODEL=gpt-5.3-codex-spark pi --print "Acknowledge: setup/testing slice model authorization and REQ-20260727-002 remains non-blocking for this slice.")"
[ -n "$model_resp" ] || fail "Model interaction returned empty response"

test -f RESUME_TEST_REPORT.md
test -f EVIDENCE_INDEX.md
[ -n "$(git diff --name-only HEAD -- RESUME_TEST_REPORT.md EVIDENCE_INDEX.md | sed '/^$/d')" ] || fail "No checkpoint/evidence file update found for this run"

if [ "$(grep -c "^### EVD-" EVIDENCE_INDEX.md)" -lt 1 ]; then
  fail "No evidence entry found in EVIDENCE_INDEX.md"
fi
latest_evd_line="$(grep -n '^### EVD-' EVIDENCE_INDEX.md | tail -n 1 | cut -d: -f1)"
[ -n "$latest_evd_line" ] || fail "Unable to locate latest EVD entry"
evidence_end=$((latest_evd_line + 60))
evidence_block="$(sed -n "${latest_evd_line},${evidence_end}p" EVIDENCE_INDEX.md)"
echo "$evidence_block" | grep -q "EVD-" || fail
echo "$evidence_block" | grep -q "Source/account alias" || fail
echo "$evidence_block" | grep -q "Command" || fail
echo "$evidence_block" | grep -q "Result" || fail
echo "$evidence_block" | grep -q "openai-codex" || fail
echo "$evidence_block" | grep -q "gpt-5.3-codex-spark" || fail
echo "$evidence_block" | grep -q "pi --list-models" || fail
echo "$evidence_block" | grep -q "pi --print" || fail

grep -qi "openai-codex" RESUME_TEST_REPORT.md
grep -qi "gpt-5.3-codex-spark" RESUME_TEST_REPORT.md
grep -qi "REQ-20260727-002" RESUME_TEST_REPORT.md
grep -qi "checkpoint" RESUME_TEST_REPORT.md

npm test
npm run validate:strict
npm run lint --if-present
npm run typecheck --if-present

git add RESUME_TEST_REPORT.md EVIDENCE_INDEX.md
if ! git diff --cached --quiet; then
  git commit -m "chore(setup): record setup/testing model authorization and checkpoint"
fi
git push origin HEAD:main

git fetch --ff-only
local_head="$(git rev-parse HEAD)"
remote_head="$(git rev-parse origin/main)"
echo "Local HEAD: $local_head"
echo "origin/main HEAD: $remote_head"
[ "$local_head" = "$remote_head" ] || fail "origin/main does not point to local HEAD after push"
git status --short --untracked-files=no
```

## phase timings

refine                7.9s
research            415.9s
  enrichment              1.3s
  worker:files wait       1.6s
  worker:files work      81.8s
  worker:apis wait        2.0s
  worker:apis work       77.9s
  worker:context wait     1.7s
  worker:context work   145.9s
  worker:tooling wait     1.7s
  worker:tooling work    11.9s
  workers               402.9s
  verify-tooling         13.0s
grill                 7.0s
  gen                     7.0s
compose              20.4s
critique             81.4s
  triage                 18.7s
  rewrite                62.7s
total               532.6s

## handoff

handoff_at: 2026-07-28T00:10:24.853Z

## gates

- 2026-07-28T00:25:14.014Z verify: FAIL — work did not verify: VERIFY script is not fully executable-to-green on this environment as provided (fails on `git fetch --ff-only`, and checkpoint diff check), so acceptance bar is not fully met despite otherwise present required artifacts/controls.
- 2026-07-28T00:29:08.639Z resolution: recommended ACCEPT
- 2026-07-28T00:29:08.645Z resolution: auto-ACCEPTED despite verify FAIL — autofix budget spent, nobody to ask (YOLO)

